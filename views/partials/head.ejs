<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= typeof title !== 'undefined' ? title : appName %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header class="site-header">
    <a href="/admin" class="brand">⚽ <%= appName %></a>
    <% if (typeof showLogout !== 'undefined' && showLogout) { %>
      <form method="post" action="/admin/logout" class="inline"><button class="link-btn">Log out</button></form>
    <% } %>
  </header>
  <main class="container">
